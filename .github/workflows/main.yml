name: main

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  lint:
    uses: spuxx1701/ci-cd/.github/workflows/npm_lint.yml@v2.1

  test:
    uses: spuxx1701/ci-cd/.github/workflows/npm_test.yml@v2.1
